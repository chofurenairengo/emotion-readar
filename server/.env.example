# ===========================================
# Comm-XR Server 環境変数設定
# ===========================================
# このファイルを `.env` にコピーして使用してください
# cp .env.example .env

# -------------------------------------------
# GCP プロジェクト設定
# -------------------------------------------
# GCPプロジェクトID（必須）
GCP_PROJECT_ID=dev-project

# -------------------------------------------
# Firebase Authentication
# -------------------------------------------
# Auth Emulator を使用する場合（ローカル開発用）
# FIREBASE_AUTH_EMULATOR_HOST=localhost:9099

# サービスアカウント認証（本番環境用、以下のいずれか1つを設定）
# 方法1: サービスアカウントキーのJSON文字列
# FIREBASE_SERVICE_ACCOUNT={"type":"service_account","project_id":"..."}

# 方法2: サービスアカウントキーファイルのパス
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account-key.json

# -------------------------------------------
# Firestore
# -------------------------------------------
# Firestore Emulator を使用する場合（ローカル開発用）
# Docker環境では compose.yml で自動設定されます
# FIRESTORE_EMULATOR_HOST=localhost:8080

# Firestore プロジェクトID（通常は GCP_PROJECT_ID と同じ）
# FIRESTORE_PROJECT_ID=dev-project

# -------------------------------------------
# LLMモデル設定
# -------------------------------------------
# ファインチューニング済みモデルのエンドポイントID（必須）
FT_MODEL_ID=
# GCP設定(必須)
# GCP_PROJECT_ID=
GCP_LOCATION=us-central1


# Vertex AI 認証（ローカル開発時に必要）
# パターン1: gcloud auth application-default login を使用
# この場合、.env から GOOGLE_APPLICATION_CREDENTIALS の行を削除またはコメントアウトする。そうすれば ADC（Application Default Credentials）が使われる。

# パターン2: サービスアカウントキーのJSON文字列を直接設定
# サービスアカウントキーのパスを指定（GOOGLE_APPLICATION_CREDENTIALS と共通）
# Cloud Run では自動でサービスアカウントから認証情報を取得するため設定不要
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account-key.json

# Vertex AI FTモデル（必須設定からgemini_client.pyで自動で作成される）
# FT_MODEL_ID=projects/my-project/locations/asia-northeast1/endpoints/1234567890

# LLM温度パラメータ（0.0〜1.0、デフォルト: 0.7）
LLM_TEMPERATURE=0.7
